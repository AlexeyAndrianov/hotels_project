<%= form_for @hotel do |hotel| %>

  <%= hotel.label :name %></br>
  <%= hotel.text_field :name %></br>

  <%= hotel.label :decription %></br>
  <%= hotel.text_field :description %></br>

  <%= hotel.submit %>
<% end %>
